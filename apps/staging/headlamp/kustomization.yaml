---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: headlamp
resources:
  - ../../base/headlamp
  - cloudflare.yaml
  # headlamp-secrets.yaml and headlamp-tunnel-credentials.yaml are created manually
  # (SOPS-encrypted) and added here once the Cloudflare tunnel and SA token exist.
