---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: homarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homarr
  template:
    metadata:
      labels:
        app: homarr
    spec:
      nodeSelector:
        kubernetes.io/hostname: hpmini02  # pinned: local-path PV lives on this node
      containers:
        - name: homarr
          image: ghcr.io/homarr-labs/homarr:v1.64.0@sha256:fdfb8ee98338feb3a48e6013ce99c048cce129ca8d6b92a7e6e66671aefa9e07
          ports:
            - containerPort: 7575
          envFrom:
            - secretRef:
                name: homarr-secrets
          volumeMounts:
            - mountPath: /appdata
              name: homarr-data
          resources:
            requests:
              cpu: "100m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          securityContext:
            allowPrivilegeEscalation: false
            runAsUser: 0  # image ships root-owned /appdata and runs as root
      volumes:
        - name: homarr-data
          persistentVolumeClaim:
            claimName: homarr-data-pvc
