---
apiVersion: v1
kind: Service
metadata:
  name: home-dashboard-backend
  labels:
    app: home-dashboard-backend
spec:
  ports:
    - port: 80
      name: http
      targetPort: 8001
    - port: 8000
      name: metrics
      targetPort: 8000
  selector:
    app: home-dashboard-backend
